---
description: Implement a Material Design 2 dashboard task with text-only browser verification
agent: dashboard-md2
---

Implement this dashboard task:

$ARGUMENTS

Use the official MUI MCP for current APIs, but keep the visual language Material Design 2. DeepSeek V4 Pro cannot see screenshots: verify responsive, accessible, light/dark, empty/error, and long-content states with source plus DOM, accessibility, computed-style, geometry, overflow, keyboard, console, and contrast evidence. Generate screenshot artifacts for human review without claiming to inspect them. Do not stage or commit.
