# Third-Party Notices

Dependency source distributions in `node_modules` contain their respective license texts. Swedish IATA ingress configuration attribution remains as documented in `config.yaml`.

Swedish MeshCore region scope names (`se`, `seXX` counties, `seXXXX` municipalities) in `src/region-scopes.ts` are based on the "Län och kommuner 2026" code list published by Statistiska centralbyrån (SCB), Statistics Sweden, with municipality names hardcoded from the official Swedish municipality list (Wikipedia, "Lista över Sveriges kommuner", 2026). SCB code lists are official public statistics.
