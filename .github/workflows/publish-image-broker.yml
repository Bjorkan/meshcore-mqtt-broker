name: Publish image broker

on:
  workflow_run:
    workflows:
      - Build image for Broker
    types:
      - completed

permissions:
  actions: read
  contents: read

jobs:
  publish:
    name: Publish image broker
    runs-on: ubuntu-latest
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main'
    steps:
      - name: Download broker image artifact
        uses: actions/download-artifact@v8
        with:
          name: broker-image.tar
          path: /tmp
          github-token: ${{ github.token }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: Prepare tags
        id: tags
        shell: bash
        env:
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail
          short_sha="${HEAD_SHA::12}"
          {
            echo "short_sha=${short_sha}"
          } >> "${GITHUB_OUTPUT}"

      - name: Load broker image
        env:
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail
          docker load --input /tmp/broker-image.tar
          docker tag "bjorkan/meshcore-mqtt-broker:build-${HEAD_SHA}" "bjorkan/meshcore-mqtt-broker:latest"
          docker tag "bjorkan/meshcore-mqtt-broker:build-${HEAD_SHA}" "bjorkan/meshcore-mqtt-broker:sha-${HEAD_SHA::12}"

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Publish broker image
        run: |
          set -euo pipefail
          docker push "bjorkan/meshcore-mqtt-broker:latest"
          docker push "bjorkan/meshcore-mqtt-broker:sha-${SHORT_SHA}"
        env:
          SHORT_SHA: ${{ steps.tags.outputs.short_sha }}
