name: Apply dashboard UI follow-up

on:
  push:
    branches:
      - fix/dashboard-ui-mobile-overhaul
    paths:
      - .github/workflows/apply-dashboard-ui-fix.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply mobile grid fix
        shell: bash
        run: |
          set -euo pipefail
          git apply --check .dashboard-ui-fix/mobile-grid.patch
          git apply .dashboard-ui-fix/mobile-grid.patch
          rm -rf .dashboard-ui-fix
          rm -f .github/workflows/apply-dashboard-ui-fix.yml
          git config user.name "OpenAI"
          git config user.email "noreply@openai.com"
          git add -A
          git commit -m "Fix mobile overview stacking"
          git push origin HEAD
